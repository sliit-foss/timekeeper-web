## Disabling Tracing

Tracing can be disabled for the application without removing existing trace functions by setting the **DISABLE_FUNCTION_TRACING** environment variable to **true** or **1**.

This is useful for deployments where tracing is not required, or where the overhead of tracing is not acceptable.
